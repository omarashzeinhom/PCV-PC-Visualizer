import { Router } from 'express';
import { addComponentToBuild } from '../controllers/component.controller';

const router = Router();

// Component routes
//router.post('/components', addComponentToBuild);

export default router;
